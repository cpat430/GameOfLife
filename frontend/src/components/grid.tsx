type GridProps = {
  rows: number;
  cols: number;
};

// export const Grid = (props: GridProps) => {
//     const { rows, cols } = props;
// }

export const Grid = (props: GridProps) => {
  const { rows, cols } = props;
};
